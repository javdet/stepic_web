upstream django {
	server 127.0.0.1:8080;
}

server {
	listen 80;
	server_name _;
	
	location ^~ /uploads {
		root /home/box/web;
	}
	location ~* ^.+\.\w+$ {
		root /home/box/web/public;
	}
	location ^~ /hello {
		uwsgi_pass django;
		include /etc/nginx/uwsgi_params;
	}

}
